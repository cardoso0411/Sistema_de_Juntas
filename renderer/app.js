// Este arquivo está obsoleto. A lógica do renderer foi migrada para renderer.js e usa Vue.
// Mantido apenas para compatibilidade; não execute automaticamente.
console.info('renderer/app.js obsoleto — use renderer/renderer.js (Vue)');